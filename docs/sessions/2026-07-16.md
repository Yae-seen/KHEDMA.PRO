# Session log — 2026-07-16 (production outage fix)

## What happened
khedma.pro was returning `404: NOT_FOUND` on every route despite the domain being
live. Diagnosed and fixed three stacked problems (site now serves 200 everywhere).

## Root causes & fixes (in order found)
1. **Vercel build failing** — `The Edge Function "middleware" is referencing
   unsupported modules: @/.` The Supabase session-refresh `middleware.ts` used the
   `@/` path alias, which Vercel's Edge bundler rejects (local `next build` was
   lenient). → Removed `middleware.ts` (`c33d563`). Re-add Edge-safe when Supabase
   is provisioned.
2. **Production branch mismatch** — Vercel's Production Branch was `main`, the repo
   is `master`. Every push deployed as a *Preview*, never promoted to Production. →
   Set Production Branch to `master` (Settings → Environments → Production).
3. **Framework Preset = `null`** (the real killer) — `get_project` returned
   `"framework": null`, so Vercel built the Next.js app but never wired its routing/
   output; the deployment's OWN `.vercel.app` URL 404'd every route. → Added
   `vercel.json` `{"framework":"nextjs"}` (`ba259e5`).

## Diagnosis method (reusable)
- DNS lookup proved the domain pointed at Vercel correctly (ruled out DNS).
- Vercel MCP `get_project` (framework:null) + `web_fetch_vercel_url` on the
  deployment's own URL (404) proved it was NOT a domain issue but the deployment
  serving nothing → framework preset.

## Verified
`curl` of www.khedma.pro/, /concours, /ar/concours, and apex khedma.pro → all 200.

## Note
- The Vercel MCP + Cloudflare MCP are now connected — use them for deploy/DNS work
  instead of the flaky dashboard browser.
- Everything from the 2026-07-15 session (Phases 2–5, design, analytics, Supabase
  backend, Arabic content) is now actually live for the first time.

## Analytics activated (same day)
Owner supplied GA4 `G-NQFV0NQGXH` and a Google tag `GT-MR5XFNM9`.

- **`GT-MR5XFNM9` is a Google tag (`gtag.js`), NOT a GTM container.** GTM containers
  are `GTM-XXXXXXX` and load via `gtm.js` + an `ns.html` `<noscript>` iframe; a `GT-`
  id put in that path would 404. The `GT-` tag routes to the same GA4 property as the
  `G-` id, so installing the `G-` measurement id alone captures all data, no double count.
- Committed the GA4 id as a **default** in `components/site-analytics.tsx` and
  `next.config.ts` (public, non-secret id — ships in page HTML regardless). This makes
  analytics live on deploy with no manual Vercel env step; env vars still override.
- CSP gotcha handled: `next.config.ts` only whitelists Google domains when a GA/GTM id
  is present, so the default had to be added in BOTH files or the tag would load but be
  CSP-blocked. Local `next build` green.
- **Owner action:** verify GA4 → Reports → Realtime shows traffic ~30 min post-deploy.

## IndexNow submitted
After the `cc9a575` deploy, `node scripts/indexnow.mjs` submitted all **55** sitemap URLs
→ HTTP 202 Accepted (Bing/Yandex recrawl). Key-file host alignment verified first
(apex `khedma.pro`, sitemap uses apex — matches `HOST` in the script).

## SEMrush Pro deep-scrape (22-agent workflow)
Ran a `Workflow` (`wf_c0bd824f-410`, 22 agents, 0 errors, ~1.3M tokens, 4.5 min) against
the now-Pro SEMrush MCP, database `ma`. Full intel: `docs/research/semrush-khedma-2026-07-16.md`.

**The findings that change the plan:**
1. **khedma.pro is a cold start** — SEMrush sees rank 561,763, **1** organic keyword
   (`khdama`, pos 23), 0 modeled traffic, AS 2, 58/60 backlinks nofollow. The "amplify
   winners" framing doesn't apply — there are no winners yet. Lever = publish winnable
   content → index → a few follow links. (GSC will show more than SEMrush; use it as truth.)
2. **Winnable core = ANAPEC + emploi public + city×emploi + مباراة التعليم long-tails**
   (KD 12–31, vol 3.6K–246K) — all on-brand for a careers site. Reprioritized in BUILD_TODO.
3. **Germany is a demand desert in Morocco** — 3 of the Germany related-keyword pulls
   returned "NOTHING FOUND"; only `visa allemagne maroc` (1.3K/KD27) has real volume.
   Reframe Germany as a lean lead-gen/monetization surface, NOT an SEO-traffic pillar.
   Build the one page with demand; validate the rest against GSC before more investment.
4. **CPC ≈ 0 everywhere** (AdSense geography trap) → revenue = pageviews × tiny RPM →
   maximize indexed/ranking pages (volume game), which is why the programmatic city cluster
   and seasonal long-tails matter.
5. **The massarservic.net mega-gap (massar/login, 5M+ vol) is the SIBLING site's job**
   (massar-service.net), not khedma.pro's — cross-link the ecosystem, don't clone login intent.

Method note: the Pro MCP works via discovery → `get_report_schema` → `execute_report`;
database `ma` is thin on FR/AR (many `NOTHING FOUND`) so treat volumes as directional.

## Competitor reframe (owner correction)
Owner: khedma.pro's competitors are **job portals** (indeed maroc, rekrute, dreamjob,
emploi-public.ma, anapec, marocannonces, ijob), NOT the Massar lookalikes — the
massarservic.net gap is the sibling site's fight. Updated §2 of the intel doc: added a
"Real competitors = job portals" table (from the emploi-related pulls) + how to beat them
(guide-grade content + tools + anti-scam vs their thin listing pages). The owner's anchor
CSV (`massar-service.net_ReferringAnchorsSimilarSite`) is useful mainly for **massar-service.net**
link-building (its column is all-zeros = ~0 anchor backlinks; moutamadris.ma is the link model).

## Logo shipped — "career case"
Replaced the placeholder K-box with a crafted brand mark: Majorelle suitcase whose saffron
handle + latch read as the start-dot of a career track (ties to the `.track-line` signature).
Bricolage wordmark, track-line hover sweep + one-time entrance (handle draw + latch pop),
reduced-motion safe. New `<Logo/>` server component in header + footer. Previewed to owner.
Commit `bd84284`. Follow-up: wire the mark into OG image + favicon.

## ANAPEC hub shipped — content backlog #1
`/blog/anapec-inscription-espace-candidat` (linked from `/emploi`, auto-listed in Guides
emploi). Targets the top win on the board: `anapec` 246K, `anapec postuler` 14.8K/KD18,
`anapec login` 3.6K/KD12. Built via a 5-agent YMYL workflow (3 web-research → draft →
adversarial verify). Grounded in loi 51-99 (NATLEX), anapec.ma (missions + réseau), the
ANAPEC anti-scam alert (Le Matin 2022). Cautious phrasing where facts are geo-restricted;
**owner follow-up: confirm exact form-field labels + login URLs during the Morocco window
(2026-08-16 → 09-04).** Content serialized from the verified workflow JSON; build green.

## Arabic jobs + Germany surface (owner: "germany and jobs are only in french")
Fixed the gap — `/emploi` and `/allemagne` were FR-only, and the Arabic nav was bouncing
Arabic users to the French pages (they weren't in `AR_SECTIONS`). Scope chosen by owner:
**jobs full, Germany lean** (data-driven: Arabic jobs terms are huge — الوظيفة العمومية 40.5K,
بوابة التشغيل العمومي 33.1K — but Arabic Germany demand is ~nil: العمل في ألمانيا 20).

Shipped:
- **`/ar/emploi`** — full Arabic jobs hub (channels, two tracks, sectors, 10 cities, CV, FAQ),
  MSA via a 2-agent translation workflow. `lib/emploi-ar.ts` label set.
- **Arabic ANAPEC guide** `/ar/blog/anapec-inscription-espace-candidat` — faithful MSA
  translation of the verified FR article; internal links mapped to `/ar/…`.
- **`/ar/allemagne`** — lean Arabic hub: explains the path in Arabic, points to the FR
  interactive tools (Chancenkarte calc / employer finder / cost estimator stay FR), lists the
  2 existing AR Germany guides, ecosystem link to massar-service.net.
- **Wiring:** `AR_SECTIONS` += /emploi, /allemagne (nav now routes to /ar/…); sitemap += both
  hubs + AR ANAPEC; hreflang on FR↔AR /emploi + /allemagne. Build green; both static.
- Gotchas handled: `FaqSection` defaults to a FR title (passed `AR_BLOG_LABELS.faqTitle`);
  `LastVerified` is FR-hardcoded (used an inline Arabic "آخر تحديث" instead).

## Owner actions #1 + #2 confirmed
Owner confirmed GA4 Realtime shows traffic AND GSC + Bing are verified with sitemap
submitted. Real data now flowing — will validate the SEMrush Germany-desert call over time.

## Content backlog #2 shipped — emploi-public.ma portal guide (FR + AR)
`/blog/emploi-public-ma-portail` + `/ar/blog/emploi-public-ma-portail`. Targets the
public-employment cluster (`emploi public` 110K, `emploi public ma` 22.2K/KD22, `بوابة
التشغيل العمومي` 33.1K/KD31). Built via a 6-agent workflow (3 web-research → draft → verify
→ AR translate). PORTAL how-to (create account, search concours, apply) — deliberately
distinct from the conceptual `trouver-emploi-public-maroc` (links to it). Grounded in the
emploi-public.ma FAQ + mmsp.gov.ma; the adversarial verify caught a real error (draft said
the activation link is valid ~24h, but that 24h is the FAQ's *password-recovery* timing →
removed) and softened "gratuit" → "aucune étape de paiement" (portal doesn't state free).
Auto-listed on /emploi + /ar/emploi; auto-hreflang + sitemap. Build green.

## Brand polish (①) + city cluster (③) + pillar (②)
Owner: "plan all the steps you mentioned and proceed" (the 4-part plan). Done:
- **① Brand**: "career case" mark wired into the OG image (`opengraph-image.tsx`) + favicon
  (`app/icon.svg`, the mark on a Majorelle tile). Commit `46f16d9`.
- **③ City × emploi cluster**: `/emploi/villes` index + `/emploi/villes/[ville]` (10 FR city
  pages). Each city individually web-researched (12-agent workflow) for DISTINCT real content —
  sectors, named zones/employers (Casanearshore, Technopolis, Fès Shore, Haliopolis, Agropolis,
  Atlantic Free Zone Kénitra, Tetouan Shore, Sidi Ghanem…), local ANAPEC, city-specific FAQ. The
  research agents HEDGED or REMOVED unconfirmed specifics (Tanger dropped Boeing/Intelcia; Oujda
  excluded Nador West Med; Rabat flagged Technopolis is in Salé) → dodges the thin-content/AdSense
  trap. `lib/city-emploi-data.ts` holds the per-city data.
- **② Pillar**: `/blog/chercher-emploi-par-ville-maroc` (FR+AR) anchors the cluster; linked from
  `/emploi` (city pills now link to the pages). Sitemap += villes index + 10 city routes. Build:
  81 static pages (was 61 at session start).
- Deferred: AR city pages (the KD-winnable city terms are French: "offre d'emploi casablanca" etc.).

## ④ AdSense/EEAT audit + fixes
6-agent audit (`docs/research/adsense-eeat-audit-2026-07-16.md`) → fixes applied via a 2-agent
workflow + direct edits:
- **P0 (AdSense risk):** the anti-scam message repeated 3× on every one of the 10 city pages
  (whereToLook tail + a near-identical FAQ + the hardcoded box) — a duplicate-content/doorway
  signal. Fixed: canonical box only; stripped the security sentence from all 10 `whereToLook`;
  replaced the duplicated FAQ with city-specific questions. Integration script validated **0**
  boilerplate-in-whereToLook, **0** `2026`-in-metaTitle, **0** duplicate FAQ across cities.
- **P1:** Tétouan real named employers (Tetouan Shore tenants, Université Abdelmalek Essaâdi,
  Sofitel Tamuda Bay; borrowed Renault line removed); brand accuracy (Sitel→Foundever ex-Sitel,
  Renault Tanger not "-Nissan"); Fès/Oujda/Tétouan intros differentiated; **modele-cv rewritten
  Morocco-specific** (FR/AR norms, photo/CIN, OFPPT labels, sector examples).
- **P2:** metaTitle years normalized (evergreen); /concours meta trimmed to ~154.

## ⚠️ Concurrent-edit note
While integrating ④, `content/blog/modele-cv-maroc.ts` + `lib/articles.ts` showed as externally
modified in the working tree (an excellent Morocco-specific CV rewrite + matching registry — the
same audit fix, done well). KEPT it (didn't overwrite) and applied only the isolated city-data fix.
Local was in sync with origin (no remote collision), but this is the "one session per repo" risk —
surface to owner.

## Content expansion + Arabic parity (owner: "keep expanding" / "the AR version is missing")
- **3 new articles (FR+AR), YMYL pipeline** (research→draft→verify→translate):
  `/blog/visa-allemagne-maroc` (backlog ⑤, 1.3K/KD27 — YMYL, hedged: fixed an Ausbildung typo,
  softened an unconfirmed 55k€ threshold, TLScontact/Rabat-embassy procedure); `/blog/trouver-stage-maroc`
  (stage maroc 1.3K/KD14); `/blog/lettre-motivation-maroc` (8.1K vol). All auto-list on their hubs
  + AR versions at `/ar/blog/...`, auto-hreflang + sitemap.
- **Arabic city cluster** (fixing "the AR version is missing"): `/ar/emploi/villes` + 10 `/ar/emploi/villes/[ville]`
  (MSA translation of the deduped FR city data; proper names kept Latin, Foundever ex-Sitel carried
  through). `lib/city-emploi-ar.ts`. Wired: FR↔AR hreflang on all city pages + index; `/ar/emploi`
  city pills now link the AR pages; sitemap += AR city routes. Nav no longer bounces AR users to FR.
- **AR CV refreshed** to match the new Morocco-specific FR (`content/ar/blog/modele-cv-maroc.ts` was stale).
- Build: **98 static pages** (was 81 → 61 at session start).

## مباراة التعليم long-tail cluster (owner: "do more AR long tails cluster concours")
Built a 4-spoke hub-and-spoke cluster around the biggest Arabic concours volume (مباراة التعليم
14.8K/KD22, التسجيل في مباراة التعليم 5.4K/KD16, مباراة التعليم الأولي ~4.3K), each FR+AR via
research→verify→translate, YMYL-hedged (platforms/dates "change each year", official men.gov.ma /
wolouj / tawdif; verify softened a dated "DEUG"→licence):
- `/blog/inscription-concours-enseignement` — التسجيل (platform, account, pièces, annual window)
- `/blog/resultats-concours-enseignement` — نتائج (présélection → écrit → définitifs par AREF)
- `/blog/preparer-concours-enseignement` — التحضير (épreuves, بطاقات توصيف, نماذج)
- `/blog/concours-enseignement-prescolaire` — التعليم الأولي (préscolaire/primaire specifics)
Distinct from the existing `concours-enseignement` REFERENCE guide — spokes link to it, and the
reference now links back to all 4 spokes (FR + AR) → full cluster interlinking. Build: **106 pages**.

## Next
- Owner: ANAPEC/emploi-public field-label check in the Morocco window (2026-08-16 → 09-04); AdSense
  (re)submit (content-quality audit clean).
- Highest-leverage lever now is **backlinks** (AS 2, 3 follow links) — outreach + massar-service.net → khedma cross-link.
